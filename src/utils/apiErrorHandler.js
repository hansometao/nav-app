/**
 * API/**
 * API 错误处理工具
 */

///**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIME/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: '/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [Error/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIME/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400)/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error -/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string}/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries -/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise}/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new Abort/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 100/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok)/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      last/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      ///**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status </**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await new Promise(resolve => setTimeout(resolve, retryDelay/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await new Promise(resolve => setTimeout(resolve, retryDelay * (attempt + 1)));
    }
  }
  
  throw/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await new Promise(resolve => setTimeout(resolve, retryDelay * (attempt + 1)));
    }
  }
  
  throw lastError;
}

/**
 */**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await new Promise(resolve => setTimeout(resolve, retryDelay * (attempt + 1)));
    }
  }
  
  throw lastError;
}

/**
 * 创建 API 请求钩子
 * @returns/**
 * API 错误处理工具
 */

// 错误类型定义
export const ErrorTypes = {
  NETWORK: 'network',
  TIMEOUT: 'timeout',
  SERVER: 'server',
  CLIENT: 'client',
  UNKNOWN: 'unknown'
};

// 错误消息映射
const errorMessages = {
  [ErrorTypes.NETWORK]: '网络连接失败，请检查网络设置',
  [ErrorTypes.TIMEOUT]: '请求超时，请稍后重试',
  [ErrorTypes.SERVER]: '服务器暂时不可用，请稍后重试',
  [ErrorTypes.CLIENT]: '请求参数错误',
  [ErrorTypes.UNKNOWN]: '发生未知错误'
};

/**
 * 分析错误类型
 * @param {Error} error - 错误对象
 * @returns {string} - 错误类型
 */
export function analyzeError(error) {
  if (!error) return ErrorTypes.UNKNOWN;
  
  // 网络错误
  if (error.name === 'TypeError' && error.message.includes('fetch')) {
    return ErrorTypes.NETWORK;
  }
  
  // 超时错误
  if (error.name === 'AbortError' || error.message.includes('timeout')) {
    return ErrorTypes.TIMEOUT;
  }
  
  // HTTP 状态码错误
  if (error.status) {
    if (error.status >= 500) return ErrorTypes.SERVER;
    if (error.status >= 400) return ErrorTypes.CLIENT;
  }
  
  return ErrorTypes.UNKNOWN;
}

/**
 * 获取用户友好的错误消息
 * @param {Error} error - 错误对象
 * @param {string} fallbackMessage - 备用消息
 * @returns {string} - 错误消息
 */
export function getErrorMessage(error, fallbackMessage = null) {
  const type = analyzeError(error);
  return fallbackMessage || errorMessages[type] || errorMessages[ErrorTypes.UNKNOWN];
}

/**
 * 带重试的 fetch 包装器
 * @param {string} url - 请求 URL
 * @param {Object} options - fetch 选项
 * @param {number} maxRetries - 最大重试次数
 * @param {number} retryDelay - 重试延迟（毫秒）
 * @returns {Promise} - fetch 结果
 */
export async function fetchWithRetry(url, options = {}, maxRetries = 3, retryDelay = 1000) {
  let lastError;
  
  for (let attempt = 0; attempt < maxRetries; attempt++) {
    try {
      const controller = new AbortController();
      const timeoutId = setTimeout(() => controller.abort(), options.timeout || 10000);
      
      const response = await fetch(url, {
        ...options,
        signal: controller.signal
      });
      
      clearTimeout(timeoutId);
      
      if (!response.ok) {
        throw new Error(`HTTP ${response.status}: ${response.statusText}`);
      }
      
      return response;
    } catch (error) {
      lastError = error;
      
      // 如果是客户端错误，不重试
      if (error.status >= 400 && error.status < 500) {
        throw error;
      }
      
      // 最后一次尝试，直接抛出错误
      if (attempt === maxRetries - 1) {
        throw error;
      }
      
      // 等待后重试
      await new Promise(resolve => setTimeout(resolve, retryDelay * (attempt + 1)));
    }
  }
  
  throw lastError;
}

/**
 * 创建 API 请求钩子
 * @returns {Object} - API 请求状态和方法
